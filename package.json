{
  "name": "axpr-timelock",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && cd ui/ && npm run build",
    "start": "cd ui/ && npm run start",
    "dev": "cd ui/ && npm run serve",
    "test": "truffle test",
    "compile": "truffle compile",
    "migrate": "truffle migrate"
  },
  "author": "scorpion9979",
  "license": "ISC",
  "dependencies": {
    "openzeppelin-solidity": "^2.3.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "dotenv": "^8.0.0",
    "truffle-hdwallet-provider": "^1.0.14"
  }
}
